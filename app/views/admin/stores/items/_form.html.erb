<%= form_for([:admin, :store, @item], html: { multipart: true }) do |form| %>
  

  <div class="field">
    <%= form.label :title %> <br />
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :description %> <br />
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :price %> <br />
    <%= form.text_field :price %>
  </div>

  <div class="field">
		<%= form.label :image %> <br />
    <%= form.cl_image_upload :image %>
  </div>
  
  <div class="category_select_box">
    <%= form.label :category %> <br />
    <%= form.select(:category_id, @categories.map { |category| [category.title, category.id] }) %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>