<div class="flex-center margin-top-5">
  <h1>New Item</h1>
</div>

<div class="flex-center">
  <%= render 'form', item: @item %>
</div>

<div class="flex-center">
  <%= link_to 'Back', admin_store_items_path(@store.slug) %>
</div>
